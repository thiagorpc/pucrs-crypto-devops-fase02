###############################################
# File: .github/workflows/007_security_dast.yml
# Name: 007. Security DAST - ZAP Scan & SonarQube Import 🛡️📊
###############################################

name: 007. Security DAST - ZAP Scan & SonarQube Import 🛡️📊

on:
  # Permite execução manual com confirmação
  workflow_dispatch:
    inputs:
      confirm_dast:
        description: "Confirmação de DAST Scan: Digite 'dast' para continuar."
        required: true
        default: ""
        type: string

  # Executa automaticamente após o deploy do frontend
  workflow_run:
    workflows: ['006. Frontend CD - Deploy UI']
    types:
      - completed

concurrency:
  group: zap_dast_scan
  cancel-in-progress: false

permissions:
  contents: read
  issues: none
  pull-requests: none


env:
  AWS_REGION: us-east-1
  APIGW_NAME: pucrs-crypto-api-gateway
  SONAR_HOST_URL: https://sonarqube.academylam.net
  SARIF_API_REPORT: zap-api-report.sarif

jobs:
  zap_dast_scan:
    name: 🛡️ Run DAST Scan & Import SonarQube
    runs-on: ubuntu-latest

    if: >
      ${{ (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.confirm_dast == 'dast') }}

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 🔐 CONFIGURAÇÃO AWS
      - name: 🔐 Configurar Credenciais AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 🎯 Obter URLs dinâmicas para o scan
      - name: 🎯 Obter Domain Name do CloudFront
        id: get_cdn_domain
        run: |
          CDN_DOMAIN=$(aws cloudfront list-distributions \
            --query "DistributionList.Items[?Comment=='CDN para o frontend React no S3'].DomainName" \
            --output text \
            --region us-east-1)
          if [ -z "$CDN_DOMAIN" ]; then echo "❌ Erro: Distribuição CloudFront não encontrada."; exit 1; fi
          echo "CDN_URL=https://$CDN_DOMAIN" >> $GITHUB_ENV
          echo "✅ CloudFront Domain: https://$CDN_DOMAIN"

      - name: 🎯 Obter DNS do API Gateway
        id: get_api_gateway_dns
        run: |
          APIGW_ID=$(aws apigateway get-rest-apis \
            --region ${{ env.AWS_REGION }} \
            --query "items[?name=='${{ env.APIGW_NAME }}'] | [0].id" \
            --output text)
          if [ -z "$APIGW_ID" ]; then echo "❌ Erro: API Gateway ID não encontrado."; exit 1; fi
          APIGW_STAGE="prod"
          APIGW_DNS_NAME="${APIGW_ID}.execute-api.${{ env.AWS_REGION }}.amazonaws.com/${APIGW_STAGE}"
          echo "APIGW_URL=https://$APIGW_DNS_NAME" >> $GITHUB_ENV
          echo "✅ API Gateway DNS: https://$APIGW_DNS_NAME"
      
      - name: 🔎 Run ZAP Full Scan - API (Geração JSON)
        uses: zaproxy/action-full-scan@v0.12.0
        with:
          target: ${{ env.APIGW_URL }}
          #rules_file_name: '.zap/rules.tsv'
          allow_issue_writing: false  # 🚫 Desativa criação de Issues automáticas
          cmd_options: '-a -m 5'       # modo agressivo e profundidade de spider

      # 📤 Upload do Relatório do Frontend
      - name: 🧾 Publicar Resumo do DAST
        if: always()
        run: |
          {
            echo "## 🛡️ DAST Scan Summary"
            echo "- Scan Concluído: ✅"
            echo "- Alvo Backend (API Gateway): ${{ env.APIGW_URL }}"
          } >> $GITHUB_STEP_SUMMARY
